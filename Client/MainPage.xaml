<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Client"
             xmlns:converter="clr-namespace:Client"
             x:Class="Client.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,10"
            Spacing="20">

            <!-- Login panel for admins -->
            <VerticalStackLayout x:Name="LoginPanel" Spacing="8">
                <Label Text="Admin Login" FontAttributes="Bold" />
                <Entry x:Name="UsernameEntry" Placeholder="Username" />
                <Entry x:Name="PasswordEntry" Placeholder="Password" IsPassword="True" />
                <HorizontalStackLayout Spacing="10">
                    <Button Text="Login" Clicked="OnLoginClickedAsync" />
                    <Button Text="Logout" Clicked="OnLogoutClickedAsync" />
                </HorizontalStackLayout>
                <Label x:Name="MessageLabel" TextColor="Red" />
            </VerticalStackLayout>

            <!-- Admin panel: new article + list. Hidden until login succeeds -->
            <VerticalStackLayout x:Name="AdminPanel" IsVisible="False" Spacing="12">
                <Button
                    x:Name="AddNew"
                    Text="New Article"
                    Clicked="OnAddNewClickedAsync"
                    HorizontalOptions="Fill" />

                <Grid Padding="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Text="Article Id" />
                    <Label Grid.Column="1" Text="Title" />
                    <Label Grid.Column="2" Text="Summary" />
                </Grid>

                <CollectionView x:Name="ArticlesView" >
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="local:ArticleDto">
                            <Grid Padding="10" BackgroundColor="{Binding Path=Expired, Converter={StaticResource colorChangedConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="0.5*" />
                                    <ColumnDefinition Width="0.5*" />
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" Text="{Binding Id}" />
                                <Label Grid.Column="1" Text="{Binding Title}" />
                                <Label Grid.Column="2" Text="{Binding Description}" />
                                <Button Grid.Column="3"
                                        Text="Delete"
                                        Clicked="OnDeleteClickedAsync" />
                                <Button Grid.Column="4"
                                        Text="Edit"
                                        Clicked="OnEditClickedAsync" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>

            <!-- Read-only section: anyone can view recent articles -->
            <VerticalStackLayout Spacing="8">
                <Label Text="Recent Articles" FontAttributes="Bold" />
                <CollectionView x:Name="PublicArticlesView">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="local:ArticleDto">
                            <StackLayout Padding="8">
                                <Label Text="{Binding Title}" FontAttributes="Bold" />
                                <Label Text="{Binding Description}" LineBreakMode="TailTruncation" />
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>